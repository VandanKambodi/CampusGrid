import { useState } from 'react';
import { ImagePlus, X } from 'lucide-react';

const eventTypes = ['Hackathon', 'Workshop', 'Seminar', 'Cultural Event', 'Sports', 'Tech Event', 'Club Event', 'Guest Lecture', 'Placement Session', 'Other'];
const statuses = ['Draft', 'Published', 'Cancelled'];
const emptyEvent = { title: '', description: '', eventType: 'Other', date: '', startTime: '', endTime: '', location: '', organizer: '', registrationRequired: false, registrationUrl: '', status: 'Published' };

function EventForm({ event, defaultDate = '', onSubmit, onClose, submitting, error }) {
  const [form, setForm] = useState(() => ({
    ...emptyEvent,
    ...(event || {}),
    date: event?.date || defaultDate,
    registrationRequired: Boolean(event?.registrationRequired)
  }));
  const [image, setImage] = useState(null);

  const update = (field, value) => setForm(current => ({ ...current, [field]: value }));
  const submit = (submitEvent) => {
    submitEvent.preventDefault();
    const payload = new FormData();
    Object.entries(form).forEach(([key, value]) => payload.append(key, value));
    if (image) payload.append('image', image);
    onSubmit(payload);
  };

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4 backdrop-blur-sm"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-gray-200 bg-white p-6 shadow-2xl dark:border-white/10 dark:bg-[#151516]"><div className="mb-5 flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-widest text-indigo-500">Campus Events</p><h2 className="text-xl font-black text-gray-900 dark:text-white">{event ? 'Edit Event' : 'Create Event'}</h2></div><button onClick={onClose} aria-label="Close event form" className="text-gray-400 hover:text-gray-900 dark:hover:text-white"><X className="w-5 h-5" /></button></div>
      {error && <div className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-xs font-semibold text-red-600 dark:border-red-500/20 dark:bg-red-500/10 dark:text-red-300">{error}</div>}
      <form onSubmit={submit} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2 text-xs font-bold text-gray-600 dark:text-gray-300">Event Title<input required value={form.title} onChange={e => update('title', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-white/5 dark:text-white" placeholder="Hackathon 2026" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Event Type<select value={form.eventType} onChange={e => update('eventType', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-white/5 dark:text-white" style={{ colorScheme: 'dark' }}>{eventTypes.map(type => <option key={type} value={type} className="bg-[#151516] text-white">{type}</option>)}</select></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Status<select value={form.status} onChange={e => update('status', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-white/5 dark:text-white" style={{ colorScheme: 'dark' }}>{statuses.map(status => <option key={status} value={status} className="bg-[#151516] text-white">{status}</option>)}</select></label><label className="sm:col-span-2 text-xs font-bold text-gray-600 dark:text-gray-300">Description<textarea value={form.description} onChange={e => update('description', e.target.value)} rows="3" className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-white/5 dark:text-white" placeholder="Enter event description" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Date<input required type="date" value={form.date} onChange={e => update('date', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Location<input value={form.location} onChange={e => update('location', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" placeholder="Seminar Hall" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Start Time<input required type="time" value={form.startTime} onChange={e => update('startTime', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">End Time<input required type="time" value={form.endTime} onChange={e => update('endTime', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" /></label><label className="text-xs font-bold text-gray-600 dark:text-gray-300">Organizer<input value={form.organizer} onChange={e => update('organizer', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" /></label><label className="flex items-center gap-2 pt-5 text-xs font-bold text-gray-600 dark:text-gray-300"><input type="checkbox" checked={form.registrationRequired} onChange={e => update('registrationRequired', e.target.checked)} className="h-4 w-4 accent-indigo-600" /> Registration required</label>{form.registrationRequired && <label className="sm:col-span-2 text-xs font-bold text-gray-600 dark:text-gray-300">Registration URL<input required value={form.registrationUrl} onChange={e => update('registrationUrl', e.target.value)} className="mt-1 w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm dark:border-white/10 dark:bg-white/5 dark:text-white" placeholder="https://..." /></label>}<label className="sm:col-span-2 flex items-center gap-2 text-xs font-bold text-gray-600 dark:text-gray-300"><ImagePlus className="w-4 h-4 text-indigo-500" /> Event image<input type="file" accept="image/*" onChange={e => setImage(e.target.files?.[0] || null)} className="block w-full text-xs font-normal" /></label></div><div className="flex justify-end gap-2 border-t border-gray-100 pt-4 dark:border-white/10"><button type="button" onClick={onClose} className="rounded-lg bg-gray-100 px-4 py-2.5 text-xs font-bold text-gray-700 dark:bg-white/5 dark:text-gray-300">Cancel</button><button disabled={submitting} className="rounded-lg bg-indigo-600 px-4 py-2.5 text-xs font-bold text-white hover:bg-indigo-700 disabled:opacity-50">{submitting ? 'Saving...' : event ? 'Save changes' : 'Create event'}</button></div></form>
    </div></div>
  );
}

export default EventForm;
